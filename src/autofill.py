import pandas as pd
import os
from browser import setup_logger, setup_webdriver, retry
# driver = setup_webdriver(headless=False, disable_extension=False)
logger = setup_logger("autofill")


if __name__ == "__main__":
    pass
